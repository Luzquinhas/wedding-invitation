@AGENTS.md

## Claude Code

- AGENTS.md (imported above) is the source of truth for design tokens, components and conventions. Do not re-read `index.html`, `css/style.css` or `js/main.js` to rediscover them; open a file only when editing it.
- Talk to the user in Brazilian Portuguese. Keep code comments in Portuguese and docs (README, AGENTS.md) in English, as they already are.
- Keep AGENTS.md in sync: when a token, component, id or behavior changes, update the matching line there in the same commit.
